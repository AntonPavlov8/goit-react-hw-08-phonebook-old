export const contactsSelector = (state) => state.root.contacts.items;
export const filterSelector = (state) => state.root.filter;
export const isLoadingSelector = (state) => state.root.isLoading;
export const errorSelector = (state) => state.root.contacts.error;
